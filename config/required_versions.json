{
  "rules_version": "v0.1.0",
  "thresholds_version": "v0.1.0",
  "normalization_version": "none",
  "notes": "Required versions for dashboard compatibility check. Update when minimum supported version changes."
}
